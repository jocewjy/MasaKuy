import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.argati.ionic.masakuy',
  appName: 'MasaKuy',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
